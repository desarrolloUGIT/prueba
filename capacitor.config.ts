import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eit.alertatempranamop',
  appName: 'emergenciaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
